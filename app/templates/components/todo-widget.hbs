{{add-todo onAdd=(action 'addTodo')}}

<ul>
  {{#each todos as |todo index|}}
  {{todo-item onClick=(action 'toggleTodo') model=todo index=index}}
  {{else}}
    You have nothing to do!
  {{/each}}
</ul>
